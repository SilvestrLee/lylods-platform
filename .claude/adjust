Read .claude/adjust and follow it exactly.

Phase 11.2 — Documentation & Test Completion

Implement the two remaining architecture gaps discovered during the documentation sync.

### Part 1 — Recover Architecture Decision Records

Recover the missing decision documents for:

- About Enterprise CMS
- Services Enterprise CMS

Requirements:

- Do not duplicate existing decision numbers.
- If numbering conflicts, renumber appropriately and update all references.
- Preserve historical implementation details.
- Ensure PROJECT.md, ROADMAP.md, ARCHITECTURE.md and any documentation references point to the correct decision files.
- No application code changes.

### Part 2 — Homepage Admin Smoke Tests

Implement permanent authenticated GET render smoke tests for the Homepage CMS editor equivalent to those already protecting About and Services.

The tests must verify:

- GET edit page returns HTTP 200
- expected admin sections render
- expected forms render
- expected "Manage Elsewhere" links render (when applicable)
- no RouteNotFoundException
- no ViewException
- no MissingVariableException

These tests should permanently protect the Home, About and Services Enterprise CMS editors against future admin rendering regressions.

Validation:

- php artisan test
- npm run build

Report:

- files created
- files modified
- validation results
- documentation updates
- rollback plan

Do not commit, merge or push.
Wait for approval.