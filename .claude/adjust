Fix the RouteNotFoundException on the About page editor.

Problem:
The About admin editor references a route named:

admin.people.team.index

but that route does not exist.

Do not create a new route just to satisfy this link.

First audit existing routes:

php artisan route:list | grep -i team
php artisan route:list | grep -i people
php artisan route:list | grep -i accreditation
php artisan route:list | grep -i organisation

Then update:

resources/views/admin/cms/pages/edit.blade.php

or the relevant About admin partial.

Replace the invalid route name with the real existing route for Team Members.

Also check the accreditation/organisation link beside it and confirm it uses a real route.

If no team management route exists, remove the button/link for now and show plain helper text instead:

"Team members are managed from the existing Team Members CMS area."

Rules:
- Do not create new routes.
- Do not create a new controller.
- Do not create a new resource.
- Do not touch public frontend.
- This is an admin-link fix only.

Validation:
php artisan route:list
php artisan optimize:clear
php artisan test
npm run build

Then open:
http://lylods.test/admin/cms/pages/2/edit

Expected:
The About page editor loads without RouteNotFoundException.